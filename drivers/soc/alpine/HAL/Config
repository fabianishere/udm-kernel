# -*-perl-*-

package.HAL = {
    interfaces = (1.0);

    deploy = {
        generic = true;
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = no-op;
    build-tools = {
        1.0 = {
            NoOpBuild = 1.0;
        };
    };
};
